---
import Layout from '../../layouts/Layout.astro';
import BrandServicePage from '../../components/pages/services/BrandServicePage.astro';
import { getLocaleFromUrl, getTranslations } from '../../i18n';

const locale = getLocaleFromUrl(Astro.url);
const t = getTranslations(locale);
---

<Layout title={t.services.veeam} description={t.servicesPages.brands.veeam.metaDescription}>
	<BrandServicePage brand="veeam" />
</Layout>