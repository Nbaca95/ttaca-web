---
import Layout from '../../layouts/Layout.astro';
import ItConsultingPage from '../../components/pages/services/Itconsultingpage.astro';
import { getLocaleFromUrl, getTranslations } from '../../i18n';

const locale = getLocaleFromUrl(Astro.url);
const t = getTranslations(locale);
---

<Layout title={t.services.itConsulting} description={t.servicesPages.itConsulting.metaDescription}>
	<ItConsultingPage />
</Layout>