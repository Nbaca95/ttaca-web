---
import Layout from '../../../layouts/Layout.astro';
import HpeAuthorizedCenterPage from '../../../components/pages/services/Hpeauthorizedcenterpage.astro';
import { getLocaleFromUrl, getTranslations } from '../../../i18n';

const locale = getLocaleFromUrl(Astro.url);
const t = getTranslations(locale);
---

<Layout title={t.services.hpeAuthorizedCenter} description={t.servicesPages.hpeAuthorizedCenter.metaDescription}>
	<HpeAuthorizedCenterPage />
</Layout>